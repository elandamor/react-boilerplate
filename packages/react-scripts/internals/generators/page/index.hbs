import React, { {{{ type }}} } from 'react';
import { Helmet } from 'react-helmet';
import classNames from 'classnames';
// Styles
import Wrapper from './styles';

// import { makeDebugger } from '@app/utils';
// const debug = makeDebugger('{{ properCase name }}');

interface I{{ properCase name }}Props {
  className?: string;
}

interface I{{ properCase name }}State {
  [key: string]: any;
}

/**
 * @render react
 * @name {{ properCase name }} page
 * @description {{ properCase name }} page.
 * @example
 * <{{ properCase name }} />
 */

class {{ properCase name }} extends {{{ type }}}<I{{ properCase name }}Props, I{{ properCase name }}State> {
  state:I{{ properCase name }}State = {}

  public render() {
    const { className } = this.props;

    return (
      <Wrapper className={classNames('', className)}>
        <Helmet>
          <title>{{properCase name}}</title>
          <meta name="description" content="Description of {{properCase name}}" />
        </Helmet>
      </Wrapper>
    );
  }
}

export default {{ properCase name }};
