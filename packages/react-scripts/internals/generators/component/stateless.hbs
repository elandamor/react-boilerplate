import React, { FC } from 'react';
import classNames from 'classnames';
// Styles
import Wrapper from './styles';

// import { makeDebugger } from '../../utils';
// const debug = makeDebugger('{{ properCase name }}');

interface IProps {
  className?: string;
};

/**
 * @render react
 * @name {{ properCase name }} component
 * @description {{ properCase name }} component.
 * @example
 * <{{ properCase name }} />
 */

const {{ properCase name }}: FC<IProps> = ({ className }) => (
  <Wrapper className={classNames('', className)} />
);

export default {{ properCase name }};
